/*
This extension ```.tfvars``` is very good using when runtime infrastructur
so you can change variable with no effecting the main.tf file. how cool is it changing on runtime hehe
so its kinda like input process? CMIIW hehe.

so this will override value on variables.tf or init with this primary value.
*/

project = "terraform-learn-gcp-446200" # Copy your work project id here. this is example
